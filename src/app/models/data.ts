export class Data {
    price: number = 0;  // Inicializar con un valor por defecto
    color: string = '';  // Inicializar con un valor por defecto
    generation: string = '';
    Capacity: number=0;
}